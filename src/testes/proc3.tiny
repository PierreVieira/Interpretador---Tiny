global z;

proc soma(a, b)
	z:= a + b;
	writeStr("Entrei na soma kkkkj");
endproc

proc main()
  local x,y;
  read(x);
  read(y);
  writeVar(x);
  writeVar(y);
  call soma(x, y);
  writeVar(z);
endproc

endp
